import { ethers } from "hardhat";

export const MAX = ethers.constants.MaxUint256;

export const AddressZero = ethers.constants.AddressZero;

export const TWO_WEEKS = 60 * 60 * 24 * 7 * 2;
export const ONE_WEEK = 60 * 60 * 24 * 7;
